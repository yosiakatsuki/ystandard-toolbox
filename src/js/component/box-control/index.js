import { __experimentalBoxControl as WPBoxControl } from '@wordpress/components';

const BoxControl = ( props ) => {
	return <WPBoxControl { ...props } __next40pxDefaultSize />;
};

export default BoxControl;
